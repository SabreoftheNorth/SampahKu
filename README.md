## Setup Development Environment

### Prerequisite
- Android Studio (sudah include JDK 21) **[Recommended]**
- ATAU JDK 21 manual + VSCode

### Jika pakai VSCode
1. Install JDK 21:
   - **Fedora**: `sudo dnf install java-21-openjdk-devel -y`
   - **Ubuntu**: `sudo apt install openjdk-21-jdk -y`
   - **macOS**: `brew install openjdk@21`
   - **Windows**: Download dari https://adoptium.net

2. Buat file `local.gradle.properties` di root project:
